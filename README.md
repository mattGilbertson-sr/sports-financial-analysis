# Sports Financial Analysis

## 🛠 Setup

1. **Install dependencies**

   From the root of the project, run:

   ```bash
   pip install -r requirements.txt
2. **Run the app**
   ```bash
   streamlit run src/main.py
